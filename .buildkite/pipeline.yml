steps:
  - name: ":docker:"
    plugins:
      docker-compose#v2.4.1:
        build: test
        image: "node:7"
        workdir: /app
  - wait

steps:
  - command: ls && node --version
    plugins:
      docker-compose#v2.4.1:
       run: test
       